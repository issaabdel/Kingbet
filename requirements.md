## Packages
framer-motion | Smooth animations for cards and page transitions
lucide-react | Icons for the UI
date-fns | Date formatting and manipulation
clsx | Utility for conditional class names
tailwind-merge | Utility for merging tailwind classes

## Notes
- The app uses a dark/neon aesthetic.
- VIP predictions are locked by default unless unlocked (simulated state).
- Admin panel is protected by a client-side PIN (20077002) for simplicity as per instructions, though real apps should use proper auth.
